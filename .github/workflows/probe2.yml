name: Probe2 Struktur
on:
  push:
    branches: [main]
  workflow_dispatch:
concurrency:
  group: probe2
  cancel-in-progress: false
jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout kode
        uses: actions/checkout@v4
      - name: Bersihkan
        run: rm -rf .git .github tests
      - name: Ganti wrangler (echo)
        run: echo "langkah deploy dilewati di probe"
